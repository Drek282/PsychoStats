Naming convention:  version_base_yyyymmdd_xx_name.sql

The "version" is the "from" version for the sql.  So this sql will convert sql for this base version.  "_" is the delimiter, so the file name cannot contain that character.

ie: 327b_base_20230122_00_mounds-of-fudge.sql
